# Copy to .env in this directory (never commit .env).
# Anthropic: https://platform.claude.com/
ANTHROPIC_API_KEY=

# Same values as ui/.env (VITE_*) — server auth reads these first; must be one project.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — defaults to Claude Sonnet 4.6 (claude-sonnet-4-6)
# AGENT_MODEL=claude-sonnet-4-6

# Optional
# SONDE_SERVER_PORT=3001
# Override auto-detection (repo ../cli from server/) when deploying elsewhere:
# SONDE_CLI_DIR=/absolute/path/to/sonde/cli

# UI (production / custom dev): point WebSocket at your agent host. Leave unset in dev — Vite proxies /agent → this server.
# VITE_AGENT_WS_URL=wss://your-api.example.com
